DEFINE CHANNEL(APP.CHANNEL) CHLTYPE(SVRCONN) TRPTYPE(TCP)
SET CHLAUTH(APP.CHANNEL) TYPE(ADDRESSMAP) ADDRESS(127.0.0.1) MCAUSER('herbert')
ALTER QMGR CONNAUTH('')
REFRESH SECURITY TYPE(CONNAUTH)

DEFINE QLOCAL(APP.DEAD)        
DEFINE QLOCAL(APP.TRANS)          
DEFINE QLOCAL(APP.FAIL)           

DEFINE QLOCAL(APP.HELLO.SERVICE)   

DEFINE QLOCAL(APP.HELLO.CLIENT.REQUEST)   
DEFINE QLOCAL(APP.HELLO.CLIENT.RESPONSE)   
