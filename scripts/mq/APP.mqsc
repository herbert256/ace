DEFINE CHANNEL(SYSTEM.INT.SVRCONN) CHLTYPE(SVRCONN) TRPTYPE(TCP)
SET CHLAUTH(SYSTEM.INT.SVRCONN) TYPE(ADDRESSMAP) ADDRESS(127.0.0.1) MCAUSER('herbert')
ALTER QMGR CONNAUTH('')
REFRESH SECURITY TYPE(CONNAUTH)

DEFINE QLOCAL(APP.DEAD)        
DEFINE QLOCAL(APP.TRANS)          
DEFINE QLOCAL(APP.FAIL)           

DEFINE QLOCAL(HELLO)   
