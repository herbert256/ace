DEFINE CHANNEL(EXT.CHANNEL) CHLTYPE(SVRCONN) TRPTYPE(TCP)
SET CHLAUTH(EXT.CHANNEL) TYPE(ADDRESSMAP) ADDRESS(127.0.0.1) MCAUSER('herbert')
ALTER QMGR CONNAUTH('')
REFRESH SECURITY TYPE(CONNAUTH)

DEFINE QLOCAL(EXT.DEAD)        
DEFINE QLOCAL(EXT.TRANS)          
DEFINE QLOCAL(EXT.FAIL)    CLUSTER(ESB)       

DEFINE QLOCAL(EXT.HELLO.RESPONSE.HTTP) CLUSTER(ESB)        
DEFINE QLOCAL(EXT.HELLO.RESPONSE.SOAP) CLUSTER(ESB)   
