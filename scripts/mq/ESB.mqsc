DEFINE CHANNEL(SYSTEM.INT.SVRCONN) CHLTYPE(SVRCONN) TRPTYPE(TCP)
SET CHLAUTH(SYSTEM.INT.SVRCONN) TYPE(ADDRESSMAP) ADDRESS(127.0.0.1) MCAUSER('herbert')
ALTER QMGR CONNAUTH('')
REFRESH SECURITY TYPE(CONNAUTH)

ALTER QMGR REPOS(ESB) 

DEFINE CHANNEL(ESB.RCVR) CHLTYPE(CLUSRCVR) TRTYPE(TCP) CONNAME('127.0.0.1(1414)') CLUSTER(ESB)
DEFINE CHANNEL(ESB.SDR)  CHLTYPE(CLUSSDR) CONNAME('127.0.0.1(1414)') CLUSTER(ESB)

DEFINE QLOCAL(ESB.DEAD)        
DEFINE QLOCAL(ESB.TRANS)          
DEFINE QLOCAL(ESB.FAIL)           

DEFINE SUB(ALL) TOPICSTR('#') PSPROP (RFH2) DEST(EVENTS.ESB)   

DEFINE QLOCAL(ESB.TST.OK.IN)    
DEFINE QLOCAL(ESB.TST.OK.OUT)   
DEFINE QLOCAL(ESB.TST.ERROR)       

DEFINE QLOCAL(HELLO.CORR)         
DEFINE QLOCAL(HELLO.SRVC)              
