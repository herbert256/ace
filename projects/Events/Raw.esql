CREATE COMPUTE MODULE Raw

	CREATE FUNCTION Main() RETURNS BOOLEAN

	BEGIN
				
		SET Environment.Dir =  'raw/' || TRIM(InputRoot.MQMD.SourceQueue) || '/'  || TRIM(InputRoot.MQRFH2.psc.Topic);	  	
		
        SET OutputRoot = InputRoot;
        		
		RETURN TRUE;
		
	END;

END MODULE;
