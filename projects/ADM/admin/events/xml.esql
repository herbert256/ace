BROKER SCHEMA events

CREATE COMPUTE MODULE XML

	CREATE FUNCTION Main() RETURNS BOOLEAN

	BEGIN

		SET OutputRoot = InputRoot;
		
		CALL shared.ClearEmptyFields (OutputRoot.XMLNSC);
		
		RETURN TRUE;

	END;

END MODULE;
