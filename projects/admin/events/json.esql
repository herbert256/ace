BROKER SCHEMA events

CREATE COMPUTE MODULE JSON
	
	CREATE FUNCTION Main() RETURNS BOOLEAN

	BEGIN

		SET OutputRoot = InputRoot;
		
		CALL Shared.ClearEmptyFields (OutputRoot.JSON);
		
		RETURN TRUE;

	END;
	
END MODULE;
